---
title: "Tipos de archivo"
description: "Qué es cada extensión de archivo que produce o consume el proyecto."
---

**Página reservada.** Una tabla de consulta con las extensiones que aparecen
en el proyecto —`.kicad_pcb`, `.kicad_sch`, `.step`, `.ipt`, `.iam`, `.dxf`,
`.glb`— y qué contiene cada una.

El dato está repartido entre las páginas de subsistema y las fichas de
artefacto. Consolidarlo es trabajo mecánico, y está pendiente.

Esta página se queda vacía a propósito. Rellenarla con texto genérico daría
la impresión de que el asunto está resuelto, y no lo está.
