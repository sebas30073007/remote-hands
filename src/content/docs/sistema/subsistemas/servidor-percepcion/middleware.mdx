---
title: Middleware ZMQ
description: Los canales entre la NUC y el visor, y los puentes serie hacia las placas — qué es, cómo funciona, con qué se conecta y su protocolo.
# Bloques grandes en el índice de la página: Sinopsis y Protocolo.
tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 2
---

import { ParameterTable, InterfaceTable, Artifact, SignalHub } from '@project';

El middleware es la parte del coordinador de la NUC que mueve mensajes: los
cuatro canales ZeroMQ con el visor y los dos puentes serie hacia las placas
embebidas.

## Sinopsis

### Qué es

Código dentro del coordinador de la NUC, no un programa aparte. Abre
**cuatro puertos ZeroMQ**, uno por tipo de dato, y mantiene dos puertos serie.
Esta página lo describe desde dentro de la NUC; el contrato entre
subsistemas, con la tabla de puertos verificados, está en [Comunicaciones del
sistema](/sistema/arquitectura/comunicaciones/#puertos-zmq-verificados).

<InterfaceTable
  caption="Puertos ZMQ del middleware de la NUC"
  columns={['Puerto', 'Rol NUC', 'Rol Unity', 'Tipo']}
  rows={[
    ['5555', 'PUB', 'SUB', 'Video: frames JPEG comprimidos'],
    ['5001', 'PUB', 'SUB', 'Sensores y estado: JSON por tópico'],
    ['5002', 'SUB', 'PUB', 'Comandos desde Unity hacia la NUC'],
    ['5007', 'PUB', 'SUB', 'Paredes y puntos LiDAR: datos binarios'],
  ]}
/>

<Artifact href="/software/nuc_master_code.py" name="Coordinador de la NUC — nuc_master_code.py" />

### Cómo funciona

Tres decisiones de diseño gobiernan el reparto:

- **Puertos separados por tipo de dato.** El video y los datos binarios no
  pueden bloquear el canal JSON, que es el que lleva el estado y los
  comandos.
- **Streaming selectivo.** Cada modo publica solo mientras está activo, así
  que un sensor apagado no cuesta ni CPU ni ancho de banda.
- ***High watermark* por canal.** Cada puerto tiene su propio límite de cola,
  y la diferencia entre ellos es deliberada.

<ParameterTable
  caption="High watermark de envío por canal"
  rows={[
    { parameter: 'Video (5555)', value: '2', unit: 'mensajes', notes: 'Un frame viejo no sirve: mejor descartarlo que acumular retraso' },
    { parameter: 'Sensores y estado (5001)', value: '300', unit: 'mensajes', notes: 'JSON pequeño; conviene no perder eventos de estado' },
    { parameter: 'Paredes y puntos (5007)', value: '50', unit: 'mensajes', notes: 'Punto medio: paquetes grandes, pero tolera algo de cola' },
  ]}
/>

El límite de **2** en video es lo que mantiene la teleoperación utilizable.
Con una cola profunda, el operador vería frames cada vez más viejos conforme
la red se degrada, en lugar de perder algunos y seguir viendo el presente.

### Conexiones

Los canales que el middleware atiende: ZeroMQ hacia el visor y serie hacia
las dos placas que reciben órdenes directas de la NUC.

<SignalHub
  centro={{ nombre: 'NUC', detalle: 'coordinador · Python 3.12', imagen: '/images/3d/nuc.webp' }}
  ramas={[
    {
      nombre: 'Meta Quest 3',
      detalle: 'cliente Unity',
      imagen: '/images/3d/meta-quest-3.webp',
      enlace: { medio: 'ZMQ por WiFi', ida: '5555 · 5001 · 5007', vuelta: '5002 · cmd JSON' },
    },
    {
      nombre: 'Puente H maestro',
      detalle: 'tracción y manipulador',
      imagen: '/images/3d/puente-h.webp',
      enlace: { medio: 'COM4 · 115 200 baud', ida: 'POSE {base} {codo} {muneca}', vuelta: 'estado' },
    },
    {
      nombre: 'Controlador del gripper',
      detalle: 'pinza',
      imagen: '/images/3d/controlador-gripper.webp',
      enlace: { medio: 'COM5 · 115 200 baud', ida: 'm {mm}', vuelta: 'GRIPPER_STATE' },
    },
  ]}
/>

## Protocolo

### Tópicos JSON (puerto 5001)

| Tópico | Frecuencia | Contenido |
| --- | --- | --- |
| `stat` | ~2 Hz | Estado global: sensores OK, modos activos, ángulos del manipulador, `gripper_mm` |
| `mode_ack` | Al cambiar modo | Confirmación de cambio de modo |
| `lidar_grid` | ~12 Hz | Grid de ocupación linealizado del modo activo |
| `manip_state` | ~4 Hz | Ángulos actuales del manipulador y finales de carrera |
| `gripper_state` | ~4 Hz | Posición del gripper en mm, `busy`, `calibrated` |
| `vision` | ~10 Hz | Resultados YOLOv8 (pose o segmentación) |
| `cam_info` | Continuo | Intrínsecos de la RealSense y escala de profundidad |

### Comandos (puerto 5002)

Todos los comandos viajan como JSON. Estos son los más relevantes, agrupados
por función:

| Familia | Ejemplo |
| --- | --- |
| Modo de percepción | `{"type": "set_camera_mode", "mode": "normal\|pose\|segment\|off"}` |
| Habilitar control | `{"type": "master_arm"}` / `{"type": "master_disarm"}` / `{"type": "stop_all"}` |
| Base móvil | `{"type": "drive_cmd", "v": 0.5, "w": -0.3, "enabled": true}` |
| Manipulador | `{"type": "manip_cmd", "q": [base_deg, codo_deg, muneca_deg]}` |
| Gripper | `{"type": "gripper_cmd", "opening_mm": 30.0}` |

`drive_cmd` usa modelo uniciclo: la NUC mezcla `v`/`w` y envía al Puente H
maestro a 15 Hz.

### Mando y seguridad

<ParameterTable
  caption="Valores que gobiernan el mando y sus paros"
  rows={[
    { parameter: 'Envío de tracción al maestro', value: '15', unit: 'Hz' },
    { parameter: 'Timeout de drive_cmd', value: '0,35', unit: 's', notes: 'Sin comando nuevo, la tracción se detiene' },
    { parameter: 'Zona muerta del joystick', value: '0,08', unit: '—', notes: 'Evita deriva por reposo impreciso del control' },
    { parameter: 'Consigna máxima al puente H', value: '255', unit: 'crudo' },
    { parameter: 'Timeout de heartbeat de comandos', value: '2,0', unit: 's' },
    { parameter: 'Consulta de estado al manipulador', value: '4', unit: 'Hz' },
    { parameter: 'Consulta de estado al gripper', value: '4', unit: 'Hz' },
    { parameter: 'Envío de comandos al gripper', value: '20', unit: 'Hz' },
    { parameter: 'Reintento de reconexión serie', value: '1,0', unit: 's' },
  ]}
/>

Los dos timeouts cubren fallos distintos. El de `drive_cmd` (0,35 s) actúa
cuando el operador deja de mandar pero la conexión sigue viva. El de
heartbeat (2 s) actúa cuando se cae el enlace entero.
