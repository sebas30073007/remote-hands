---
title: Controlador CL57T
description: La placa que mueve los tres ejes del manipulador — qué es, cómo funciona, cómo se conecta, su PCB y su firmware.
# Bloques grandes en el índice de la página: Sinopsis, Drivers y motores, PCB y Firmware.
tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 2
---

import { ParameterTable, InterfaceTable, Artifact, SignalChain } from '@project';

<figure>
  <img src="/images/3d/controlador-cl57t.webp" alt="Placa del controlador CL57T, con su módulo ESP32-C3 y sus borneras" />
</figure>

El controlador CL57T es la placa que mueve los tres ejes del manipulador:
base, codo y muñeca. Recibe de la NUC la posición a la que tiene que ir cada
eje y la convierte en los pulsos que hacen girar sus motores.

## Sinopsis

### Qué es

Una placa de diseño propio basada en un **ESP32-C3**. No mueve los motores
por sí misma: genera las señales de paso (`PUL`) y dirección (`DIR`)
para tres drivers **CL57T** comerciales, que son los
que entregan la potencia a cada motor NEMA17.

Comparte arquitectura con el módulo Puente H —ESP32-C3, aislamiento óptico,
conectores I²C encadenables— pero sin etapa de potencia: de esta placa solo
salen señales lógicas. Por eso es más pequeña y usa optoacopladores PC817 en
lugar de 4N25.

<ParameterTable
  caption="Driver CL57T"
  rows={[
    { parameter: 'Motor compatible', value: 'NEMA17', unit: '—' },
    { parameter: 'Tipo de lazo', value: 'Cerrado', unit: '—', notes: 'encoder incremental' },
    { parameter: 'Señales desde esta placa', value: 'PUL, DIR', unit: '—', notes: 'detalle en Drivers y motores' },
  ]}
/>

### Cómo funciona

La NUC no le habla directamente. Manda la orden al Puente H maestro por USB,
y el maestro la reenvía por I²C a esta placa, que escucha en la dirección
`0x0B`. Cada orden es una línea de texto —`CODO_GOTO <grados>`,
`POSE <base> <codo> <muñeca>`— y la placa responde cuando el movimiento
termina.

Para cada eje convierte el ángulo pedido en pasos, según la reducción de su
transmisión, y los emite con una rampa de aceleración y frenado. En cada
encendido busca el cero del codo y de la muñeca con sus finales de carrera.

<ParameterTable
  caption="Resolución por eje (incluye reducción HTD3M + planetario 10:1 en codo y muñeca)"
  rows={[
    { parameter: 'Base', value: '10 000', unit: 'pasos/vuelta', notes: '−80° a +80° · 27,78 pasos/°' },
    { parameter: 'Codo', value: '25 000', unit: 'pasos/vuelta', notes: '0° a 136,5° · 69,44 pasos/°' },
    { parameter: 'Muñeca', value: '25 000', unit: 'pasos/vuelta', notes: '−220° a 0° · 69,44 pasos/°' },
  ]}
/>

### Conexiones

Del operador al motor. Cada tramo lleva el medio por el que viaja la orden.

<SignalChain
  pasos={[
    { nombre: 'Meta Quest 3', detalle: 'interfaz XR', imagen: '/images/3d/meta-quest-3.webp' },
    { nombre: 'NUC', detalle: 'Python 3.12', imagen: '/images/3d/nuc.webp' },
    { nombre: 'Puente H maestro', detalle: 'ESP32-C3', imagen: '/images/3d/puente-h.webp' },
    {
      nombre: 'Controlador CL57T',
      detalle: 'ESP32-C3',
      imagen: '/images/3d/controlador-cl57t.webp',
      ramas: [
        {
          nombre: 'Finales de carrera × 2',
          detalle: 'codo y muñeca',
          imagen: '/images/3d/final-de-carrera.webp',
          enlace: { medio: 'SW2 GPIO3 · SW3 GPIO4', vuelta: 'contacto en el tope' },
        },
      ],
    },
    { nombre: 'Driver CL57T × 3', detalle: 'lazo cerrado', imagen: '/images/3d/driver-cl57t.webp' },
    { nombre: 'NEMA17 + reductor planetario × 3', detalle: 'base, codo y muñeca', imagen: '/images/3d/nema17-reductor.webp' },
  ]}
  enlaces={['ZMQ · cmd (manip_cmd)', 'Serial · COM4', 'I²C 0x0B · texto ASCII', 'PUL / DIR', '']}
/>

Del controlador cuelgan los **dos finales de carrera** que lee el firmware,
los del codo y la muñeca. La base tiene el suyo en la entrada `SW1`, pero el
firmware no lo lee. Sus entradas están en [Asignación de
pines](#asignación-de-pines).

## Drivers y motores

Los actuadores del brazo son tres pares driver–motor, uno por eje. Esta
placa solo les manda pulsos: la potencia la entregan los drivers.

### Driver CL57T

<figure>
  <img src="/images/3d/driver-cl57t.webp" alt="Driver de motor paso a paso CL57T" />
</figure>

Un driver comercial de motor paso a paso **de lazo cerrado**: además de
mover el motor, lee su encoder y corrige la posición si el motor pierde
pasos por carga. Por eso el brazo puede trabajar con motores de laboratorio
sin que un tirón lo descuadre.

Recibe dos señales de esta placa por eje:

- **PUL**, un pulso por paso. En reposo la línea queda en alto y cada paso
  es un pulso en bajo; el bajo y el alto duran lo mismo, así que el valor
  de rampa del firmware es medio periodo.
- **DIR**, el sentido. Tras cambiarla, el firmware espera 200 µs antes del
  primer pulso para que el driver la vea estable.

El firmware no conmuta la entrada de habilitación: en la tabla de pines no
hay GPIO para `ENABLE`. Tampoco lee la salida de alarma del driver.

<ParameterTable
  caption="Configuración que asume el firmware"
  rows={[
    { parameter: 'Pulsos por vuelta del motor', value: '1 000', unit: 'pulsos', notes: 'deducido: 10 000 ÷ 10 en la base, 25 000 ÷ 25 en codo y muñeca' },
    { parameter: 'Pulsos por vuelta de la base', value: '10 000', unit: 'pulsos', notes: 'reducción total 10:1' },
    { parameter: 'Pulsos por vuelta de codo y muñeca', value: '25 000', unit: 'pulsos', notes: 'reducción total 25:1' },
  ]}
/>

Los micropasos se fijan con los DIP switches de cada driver, y deben dar
esos **1 000 pulsos por vuelta**. Con otro ajuste el brazo se mueve, pero
los ángulos que reporta dejan de coincidir con los reales. La corriente de
cada driver no está documentada en el proyecto.

Los tres drivers van dentro de la caja de elevación, sobre el soporte de
drivers.

<figure>
  <img src="/images/build/conexiones-driver-cl57t.webp" alt="Conexiones de los drivers CL57T" />
  <figcaption>Cableado de los drivers CL57T, alojados dentro de la estructura en «C» que eleva el manipulador.</figcaption>
</figure>

### NEMA 17 con reductor planetario

<figure>
  <img src="/images/3d/nema17-reductor.webp" alt="Motor NEMA 17 con reductor planetario acoplado" />
</figure>

Cada eje lo mueve un motor paso a paso **NEMA 17** —brida cuadrada de 42 mm—
con un **reductor planetario 10:1** acoplado a su eje. Los motores no se
eligieron para esta aplicación: eran el stock del laboratorio, y la
reducción es lo que les da el par que el brazo necesita. En codo y muñeca,
la banda HTD3M añade otro 2,5:1, como se explica en
[Mecanismo](/sistema/subsistemas/robot-movil/manipulador/mecanismo/).

<InterfaceTable
  caption="Motor y reducción por eje"
  columns={['Eje', 'Reducción total', 'Rango', 'Pasos por grado']}
  rows={[
    ['Base', '10:1 (planetario)', '−80° a +80°', '27,78'],
    ['Codo', '25:1 (planetario × banda)', '0° a 136,5°', '69,44'],
    ['Muñeca', '25:1 (planetario × banda)', '−220° a 0°', '69,44'],
  ]}
/>

El modelo de referencia del CAD es un NEMA 17 de 4,4 kg·cm; el número de
parte de los motores montados no está documentado.

<Artifact href="/cad/reference/nema17.step" name="STEP — Motor NEMA 17 (referencia)" />
<Artifact href="/cad/reference/reductor_nema17.stp" name="STEP — Reductor planetario NEMA 17 (referencia)" />

### Velocidades resultantes

Con 1 000 pulsos por vuelta y las reducciones de cada eje, los tiempos del
firmware dan estas velocidades:

<InterfaceTable
  caption="Velocidad por fase de movimiento, calculada del firmware"
  columns={['Fase', 'Medio periodo', 'Pasos/s', 'Motor', 'Base', 'Codo y muñeca']}
  rows={[
    ['Crucero', '1 600 µs', '312,5', '18,75 rpm', '11,25 °/s', '4,5 °/s'],
    ['Arranque y frenado', '2 500 µs', '200', '12 rpm', '7,2 °/s', '2,88 °/s'],
    ['Homing rápido', '1 800 µs', '277,8', '16,7 rpm', '—', '4,0 °/s'],
    ['Homing fino', '3 200 µs', '156,3', '9,4 rpm', '—', '2,25 °/s'],
  ]}
/>

Las rampas de 1 500 pasos equivalen a 54° en la base y a 21,6° en codo y
muñeca. Un recorrido completo del codo, de 0° a 136,5°, tarda del orden de
medio minuto.

## PCB

### La placa

<figure>
  <img
    src="/images/esquematico-controlador-drivers.png"
    alt="Esquemático en KiCad del controlador CL57T"
  />
  <figcaption>Esquemático: el ESP32-C3, las salidas PUL/DIR hacia los tres drivers y las entradas de final de carrera.</figcaption>
</figure>

<ParameterTable
  caption="Especificación de fabricación"
  rows={[
    { parameter: 'Dimensiones', value: '56,4 × 59,0', unit: 'mm' },
    { parameter: 'Capas de cobre', value: '2', unit: 'F.Cu / B.Cu' },
    { parameter: 'Espesor de placa', value: '1,6', unit: 'mm' },
    { parameter: 'Vías', value: '16', unit: '—' },
    { parameter: 'Ancho de pista', value: '0,3 – 1,0', unit: 'mm', notes: 'Sin pistas de potencia: no las necesita' },
  ]}
/>

<InterfaceTable
  caption="Lista de materiales"
  columns={['Designadores', 'Cant.', 'Valor', 'Encapsulado']}
  rows={[
    ['ESP32-C3', 1, 'ESP32-C3 SuperMini', 'Módulo SMD'],
    ['U1–U6', 6, 'PC817', 'DIP-4, zócalo SMD'],
    ['VP_IN1–VP_IN4', 4, 'Bornera de salida a driver', 'MX126-5.0, 2 P'],
    ['DATA_IN1, DATA_OUT1', 2, 'Conector I²C', 'JST EH 4 P vertical'],
    ['CONN01X06', 1, 'Header HC-05', 'JST EH 6 P vertical'],
    ['R1, R3, R4, R7, R9–R11', 7, '330 Ω', '1206'],
    ['R20, R21', 2, '4,7 kΩ', '1206'],
    ['R16', 1, '10 kΩ', '1206'],
    ['R5', 1, '3,3 kΩ', '1206'],
    ['C1, C3', 2, '100 nF', '1206'],
    ['D2, D3', 2, 'LED de datos y de potencia', '1206'],
    ['SW2', 1, 'Pulsador', 'RS-032G05A3-SMRT'],
  ]}
/>

La placa lleva **cuatro** borneras de salida para tres ejes: la cuarta quedó
como reserva y no se usa en esta configuración.

<figure>
  <img
    src="/images/pcb-2-capas-controlador-drivers.png"
    alt="Layout de PCB de dos capas del controlador CL57T"
  />
  <figcaption>Layout de dos capas.</figcaption>
</figure>

<Artifact href="/pcb/controlador-drivers/gerbers.zip" name="Gerbers de fabricación — Controlador CL57T" />
<Artifact href="/pcb/controlador-drivers/bom.csv" name="Lista de materiales — Controlador CL57T (CSV)" />

#### Costo de fabricación

<figure>
  <img
    src="/images/manufactura-jlcpcb-controlador-drivers.png"
    alt="Vista de manufactura de JLCPCB del controlador CL57T"
  />
  <figcaption>La vista de manufactura que devuelve JLCPCB al subir los Gerbers: la última revisión antes de pagar el lote.</figcaption>
</figure>

Cifras reales de JLCPCB para un lote de 10 piezas de dos capas, sin ensamble
de fábrica. El Puente H cae en el mismo rango.

| Concepto | Costo aproximado |
| --- | --- |
| Producción | 5,00 USD |
| Envío | 17,50 USD |
| Derechos de importación | 130 MXN |

El envío pesa más del triple que la producción: conviene pedir las dos
placas del proyecto en el mismo lote.

### Asignación de pines

| Señal | GPIO | Función |
| --- | --- | --- |
| `PUL_BASE` / `DIR_BASE` | GPIO20 / GPIO21 | Base |
| `PUL_CODO` / `DIR_CODO` | GPIO7 / GPIO10 | Codo |
| `PUL_MUNECA` / `DIR_MUNECA` | GPIO5 / GPIO6 | Muñeca |
| `SW1` | GPIO1 | Final de carrera de la base |
| `SW2` / `SW3` | GPIO3 / GPIO4 | Finales de carrera de codo / muñeca |
| `SDA` / `SCL` | GPIO8 / GPIO9 | Bus I²C esclavo (dirección `0x0B`) |
| `BTN` | GPIO0 | Botón local (homing sin NUC) |

## Firmware

El firmware es MicroPython sobre el ESP32-C3. Atiende las órdenes por I²C y
también por USB serie, que queda siempre disponible para depurar.

<Artifact href="/firmware/main_manipulador_final.py" name="Firmware — main_manipulador_final.py" />
<Artifact href="/software/calibracion_manipulador.py" name="Script de calibración del manipulador" />

### Protocolo ASCII por I²C

<InterfaceTable
  caption="Comandos principales — todos bloquean hasta que el movimiento termina"
  columns={['Comando', 'Argumento', 'Respuesta', 'Descripción']}
  rows={[
    ['PING', '—', 'PONG', 'Verificar que el esclavo responde'],
    ['STATE?', '—', 'OK busy=0 sw2=0 sw3=0 calibrated=1 base=… codo=… muneca=…', 'Estado completo'],
    ['HOME_ALL', '—', 'OK / ERR HOME_ALL', 'Homing de codo + muñeca'],
    ['BASE_GOTO', 'grados', 'OK BASE …', 'Posición absoluta de base'],
    ['CODO_GOTO', 'grados', 'OK CODO …', 'Posición absoluta de codo'],
    ['MUNECA_GOTO', 'grados', 'OK MUNECA …', 'Posición absoluta de muñeca'],
    ['POSE', 'base codo muneca', 'OK POSE …', 'Los 3 ejes en una sola trama'],
  ]}
/>

### Rutina de homing

El codo y la muñeca buscan su cero en cada encendido, en cuatro fases:

```text
Fase 1 — Búsqueda rápida:  avanza hacia el switch (medio periodo 1 800 µs)
Fase 2 — Backoff:          retrocede 220 pasos (medio periodo 3 200 µs)
Fase 3 — Búsqueda fina:    avanza de nuevo, lento (medio periodo 3 200 µs)
Fase 4 — Clearance final:  retrocede 120 pasos → posición cero reproducible
```

La base tiene su propio final de carrera, en la entrada `SW1`, pero el
firmware actual no lo lee: la base no hace homing y arranca desde la
posición en la que haya quedado.

Una pulsación larga del botón físico (GPIO0) ejecuta `HOME_ALL` sin
intervención de la NUC. Esa vía sirve durante el desarrollo y para recuperar
el brazo tras un error. La lectura de cada final de carrera aplica un
debounce de 5 ms con doble confirmación.

Fuera del homing, los finales de carrera también protegen: si durante
cualquier movimiento se activa el del codo o el de la muñeca, el movimiento
se detiene y la placa responde `ABORT`.

### Compensación mecánica de muñeca

La transmisión de correa HTD3M entre el codo y la muñeca acopla físicamente
ambos ejes: cuando el codo gira, la muñeca gira con él aunque nadie se lo
haya ordenado.

El firmware compensa ese arrastre emitiendo pulsos hacia el motor de muñeca
en paralelo con los del codo. El efecto es que las dos variables se
separan: el ángulo lógico que ve el operador (`pos_muneca`) permanece
estable, mientras la posición física del motor (`pos_muneca_motor`) sí
cambia.

### Rampas de velocidad

Cada eje usa su propio perfil trapezoidal: aceleración lineal, crucero y
deceleración lineal. Esa forma de arrancar y frenar evita la pérdida de pasos
al inicio y la vibración al detenerse.

Los tres ejes comparten los mismos valores. Cada valor es **medio periodo**
del pulso —lo que dura en bajo, y lo mismo en alto—, así que un valor mayor
significa ir más despacio: el movimiento arranca con 2 500 µs, acelera hasta
1 600 µs de crucero y vuelve a 2 500 µs para frenar. Las velocidades que
resultan están en [Velocidades resultantes](#velocidades-resultantes).

<ParameterTable
  caption="Perfil trapezoidal — idéntico en base, codo y muñeca"
  rows={[
    { parameter: 'Medio periodo de arranque', value: '2500', unit: 'µs', notes: '200 pasos/s' },
    { parameter: 'Medio periodo de crucero', value: '1600', unit: 'µs', notes: '312,5 pasos/s' },
    { parameter: 'Medio periodo de frenada', value: '2500', unit: 'µs', notes: '200 pasos/s' },
    { parameter: 'Pasos de aceleración', value: '1500', unit: 'pasos' },
    { parameter: 'Pasos de deceleración', value: '1500', unit: 'pasos' },
    { parameter: 'Asentamiento tras cambio de DIR', value: '200', unit: 'µs', notes: 'El driver necesita ver DIR estable antes del primer pulso' },
  ]}
/>

La base añade una regla propia: si un movimiento cruza el cero y recorre más
de **60°**, se parte en dos tramos. Un giro largo de la base a través del
origen mueve todo el brazo, y partirlo evita el tirón en el punto de cruce.

### Prueba desde tu PC

La placa acepta por USB los mismos comandos que recibe por I²C, así que se
prueba sin el Puente H maestro: basta conectar su USB-C a una PC. El script
consulta el estado y, si se lo pides, hace el homing y un vaivén corto de la
base. Cada comando de movimiento bloquea hasta terminar.

1. Conecta el USB-C del ESP32-C3 a la PC y busca su puerto COM.
2. Para mover: drivers CL57T alimentados y el brazo libre de obstáculos.
3. Instala la dependencia: `pip install pyserial`.
4. Corre la prueba:

```bash
python prueba_controlador_cl57t.py --puerto COM6
python prueba_controlador_cl57t.py --puerto COM6 --home --mover
python prueba_controlador_cl57t.py --puerto COM6 --consola
```

<Artifact href="/software/pruebas/prueba_controlador_cl57t.py" name="Prueba del Controlador CL57T — prueba_controlador_cl57t.py" />

La placa debe responder `PONG` y una línea `OK busy=0 …` con los ángulos y
el estado de los finales de carrera. Tras `HOME_ALL`, `calibrated=1`.
