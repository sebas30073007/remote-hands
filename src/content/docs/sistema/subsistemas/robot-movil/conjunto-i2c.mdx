---
title: Conjunto I²C
description: Las tres placas que la NUC controla por un solo USB — Puente H maestro, Puente H esclavo y controlador CL57T.
tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 2
---

import { InterfaceTable, SignalHub } from '@project';

<figure>
  <img src="/images/3d/portada-conjunto-i2c.webp" alt="Los dos módulos Puente H encimados y el controlador CL57T con un final de carrera" />
</figure>

La NUC mueve la tracción y el manipulador con un solo cable USB. Lo conecta
al Puente H maestro, y el maestro reparte las órdenes por un bus I²C a las
otras dos placas.

## Sinopsis

### Qué es

Tres placas de diseño propio, cada una con un ESP32-C3, encadenadas por I²C.
Solo el maestro tiene conexión directa a la NUC; las otras dos cuelgan de él
en serie.

<InterfaceTable
  caption="Placas del conjunto"
  columns={['Placa', 'Conexión', 'Protocolo', 'Mueve']}
  rows={[
    ['Puente H maestro', 'USB-CDC COM4 a la NUC', '—', 'Motor DC derecho'],
    ['Puente H esclavo', 'I²C 0x08, desde el maestro', 'Binario, tramas de 7 bytes', 'Motor DC izquierdo'],
    ['Controlador CL57T', 'I²C 0x0B, desde el maestro', 'Texto ASCII', 'Los 3 ejes del manipulador'],
  ]}
/>

### Cómo funciona

El Puente H maestro se configura como tal con su DIP switch en `100`. A
partir de ahí, la NUC le habla por USB y él decide a quién va cada orden: la
de tracción la reparte entre su propio motor y el del esclavo; la del brazo
la reenvía al controlador CL57T.

Los dos esclavos hablan protocolos distintos: el Puente H esclavo recibe
tramas binarias de 7 bytes, y el controlador CL57T, líneas de texto ASCII.

El controlador del gripper no forma parte del conjunto: va a la NUC por su
propio USB (COM5).

Cada placa está documentada en su página: [Puente
H](/sistema/subsistemas/robot-movil/electronica/) y [Controlador
CL57T](/sistema/subsistemas/robot-movil/controladores/cl57t/).

### Conexiones

El Puente H maestro es el centro del conjunto: recibe de la NUC y reparte
por el bus.

<SignalHub
  centro={{ nombre: 'Puente H maestro', detalle: 'ESP32-C3 · DIP 100', imagen: '/images/3d/puente-h.webp' }}
  ramas={[
    {
      nombre: 'NUC',
      detalle: 'Python 3.12',
      imagen: '/images/3d/nuc.webp',
      enlace: { medio: 'USB-CDC · COM4', vuelta: 'tracción · POSE', ida: 'estado' },
    },
    {
      nombre: 'Puente H esclavo',
      detalle: 'motor izquierdo',
      imagen: '/images/3d/puente-h.webp',
      enlace: { medio: 'I²C 0x08', ida: 'tramas binarias de 7 bytes' },
    },
    {
      nombre: 'Controlador CL57T',
      detalle: 'tres ejes del brazo',
      imagen: '/images/3d/controlador-cl57t.webp',
      enlace: { medio: 'I²C 0x0B', ida: 'POSE · HOME_ALL', vuelta: 'OK …' },
    },
  ]}
/>

Estas son las direcciones del bus que fija el firmware del maestro, y las que
hay que respetar al añadir un nodo.

<InterfaceTable
  caption="Direcciones I²C del bus, según el firmware de tracción"
  columns={['Dirección', 'Nodo', 'Protocolo']}
  rows={[
    ['0x08', 'Puente H esclavo 1: motor izquierdo', 'Binario, tramas de 7 bytes'],
    ['0x09', 'Puente H esclavo 2: reservado', 'Binario, tramas de 7 bytes'],
    ['0x0A', 'Puente H esclavo 3: reservado', 'Binario, tramas de 7 bytes'],
    ['0x0B', 'Controlador CL57T', 'Texto ASCII'],
  ]}
/>

Las direcciones `0x09` y `0x0A` las reserva el DIP switch del Puente H, pero
no se usan en esta configuración.
